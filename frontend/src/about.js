/**
 * このマップを作った理由（企画意図）。
 *
 * 避難所の条件設定の横に置いた人アイコンから開く。
 * 何ができるかは画面を触れば分かるが、「なぜ作ったか」は書かないと伝わらないため、
 * 作者本人の言葉をそのまま載せている。読みやすさのため段落だけ分けてある。
 */

export const ABOUT_TITLE = 'このマップを作った理由';

/**
 * 原文の言い回しは変えない。表示の都合で書き換わっていないことは
 * `frontend/tests/about.test.js` で固定している。
 *
 * ただし医療機関の動機については、家族の病歴が公開ページに残るのを避けるため
 * 作者の判断で「家族が疾患をもっている」まで一般化してある。動機の説得力は保ちつつ、
 * 個人が特定される情報を載せない。具体的な続柄・病名は書き戻さないこと。
 */
export const ABOUT_PARAGRAPHS = [
  'このマップの企画意図ですが、災害が発生したときに自分の家の近くの避難所が混むかどうかを確認したいとおもい、人口と避難所が表示されるようにしたいとおもい、作成しました。',
  'また、家族が疾患をもっているため、災害時に頼れる病院も可視化させたいと思いました。',
  'そのあと、災害発生からすこし時間が経過して、家の水道設備が壊れてトイレが使えないとかの場合、近くのトイレが表示されたらいいかな？とか、家さがしのときに災害のときに強く地価が安いところを探す手がかりになれば、自分の資産を守れると思い、機能を設計いたしました。',
];

/**
 * 吹き出しの開閉状態を反転する。
 *
 * `aria-expanded` は属性なので文字列で返り、未設定なら null になる。
 * どちらも「閉じている」とみなして、1回目の押下で必ず開くようにする。
 *
 * @param {boolean|string|null|undefined} current 現在の開閉状態
 * @returns {boolean} 次の開閉状態
 */
export function nextAboutState(current) {
  const isOpen = current === true || current === 'true';
  return !isOpen;
}
