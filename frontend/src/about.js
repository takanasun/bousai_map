/**
 * このマップを作った理由（企画意図）。
 *
 * 避難所の条件設定の横に置いた人アイコンから開く。
 * 何ができるかは画面を触れば分かるが、「なぜ作ったか」は書かないと伝わらないため、
 * 作者本人の言葉をそのまま載せている。読みやすさのため段落だけ分けてある。
 */

export const ABOUT_TITLE =
  'いまの暮らしの「安心」も、これからの住まいの「安心」も、この一つのマップで。';

/**
 * 本文。作者本人の言葉なので、表示の都合で書き換えないこと。
 * 内容が変わっていないことは `frontend/tests/about.test.js` で固定している。
 *
 * 一般公開するページのため、家族が特定される情報は載せない。
 * 「持病のある家族」以上に具体的な続柄・病名は書き戻さないこと。
 */
export const ABOUT_PARAGRAPHS = [
  'このマップは、開発者自身の「災害から大切な家族を守りたい、安心して暮らしたい」という想いから生まれました。',
  '「近くの避難所は混雑していないか？」「持病のある家族をすぐに連れていける病院はどこか？」災害直後のそんな不安を解消するために、周辺の人口データ、避難所、医療機関を可視化しています。',
  'さらに、このマップはこれから新生活を始める方のお手伝いも出来ます。単身でも家族でも、災害リスクが低く避難場所が充実しているエリアを一目で探すことが出来ます。',
  '今住んでいる街で、次の住む街で、「安心」を探す時に1つの指標として是非ご活用ください。',
];

/**
 * 吹き出しの開閉状態を反転する。
 *
 * `aria-expanded` は属性なので文字列で返り、未設定なら null になる。
 * どちらも「閉じている」とみなして、1回目の押下で必ず開くようにする。
 *
 * @param {boolean|string|null|undefined} current 現在の開閉状態
 * @returns {boolean} 次の開閉状態
 */
export function nextAboutState(current) {
  const isOpen = current === true || current === 'true';
  return !isOpen;
}
