/**
 * 地図の初期表示設定。
 */

/**
 * 既定の基準地点：神奈川県庁（横浜市中区日本大通1）。
 *
 * 現在地が取得できず、住所も指定されていない場合の出発点。
 * 特定個人の自宅を既定値として埋め込むと、他の利用者にとって
 * 無関係な場所が基準になってしまうため、県の中心的な公共施設を使う。
 */
export const DEFAULT_CENTER = { lat: 35.4478, lng: 139.6425 };

/** 既定地点の表示名。ラベルやステータスに出す。 */
export const DEFAULT_CENTER_NAME = '神奈川県庁';

/**
 * 初期ズームレベル。
 *
 * 緯度35.5度・ズーム14 で約 7.8 m/px となり、幅800px の画面に約6.2km が入る。
 * 半径1.5km（直径3km）の避難所が見渡せる。
 */
export const INITIAL_ZOOM = 14;

/** 初期表示で見渡せることを意図している半径（km）。凡例の表示に使う。 */
export const VIEW_RADIUS_KM = 1.5;
