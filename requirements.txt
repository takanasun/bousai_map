# =============================================================================
# ランタイム依存 (Azure Functions にデプロイされるもの)
# コールドスタートを意識し、依存は最小限に保つ。
# データ加工用の重量級ライブラリ(pandas 等)はここに入れず、
# requirements-dev.txt 側に置くこと。
# =============================================================================
azure-functions>=1.18.0

# 外部API呼び出し用（Azure Maps 等。ローカルではモックを使用するが、
# 将来の本番実装で使用するため先行して固定しておく）
requests>=2.31.0
